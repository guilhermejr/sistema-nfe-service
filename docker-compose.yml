services:
  nfe-service:
    image: guilhermejr/sistema-nfe-service
    container_name: nfe-service
    restart: always
    ports:
      - "9011:9011"

networks:
  default:
    external: true
    name: rede
